package models

import (
{{- range .Imports }}
    "{{ . }}"
{{- end }}
)

type {{ .Model.Name }} struct {
{{- range .Model.Fields }}
    {{ toCamelCase .Name }} {{ .Type }} {{ printf "`json:\"%s\"`" .Name }}
{{- end }}
}
